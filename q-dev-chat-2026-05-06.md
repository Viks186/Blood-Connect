<div style="display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 200px 0 20px 0;">

<div style="font-size: 24px; margin-bottom: 12px;"><strong>Amazon Q</strong></div>
<div style="background: rgba(128, 128, 128, 0.15); border: 1px solid rgba(128, 128, 128, 0.25); border-radius: 8px; padding: 8px; margin: 4px 0; text-align: center;">
<div style="font-size: 14px; margin-bottom: 4px;"><strong>Did you know?</strong></div>
<div>Create and add Saved Prompts using the @ context menu</div>
</div>

Select code & ask me to explain, debug or optimize it, or type `/` for quick actions

</div>

---

hey there is some issues in this projects i am providing you the description please analyze all files and description and tell me the changes






### 📋 Overview

 The README promises donors see **matching** blood requests. The system fulfils this with a naive exact filter — which is **medically incorrect** and breaks the platform&#39;s core promise. `O−` donors never see `AB+` requests. `AB+` patients never find `O−` donors. The feature is broken by design. on `blood_group` + `rh_factor`. This is **medically incorrect** — blood compatibility follows a strict donation matrix, not simple equality.

Key gaps today:
- `O−` (Universal Donor) can donate to **all 8 types** but the system only shows them to `O−` patients
- `AB+` (Universal Recipient) can receive from **all 8 types** but only sees `AB+` donors
- **RH factor rules are entirely unimplemented** — `RH−` patients must never receive `RH+` blood
- Donors are **not ranked** — no scoring, no distance weighting, completely arbitrary order

---

### ❌ Problem Statement

| # | Problem | Affected File |
|---|---|---|
| 1 | Exact-match filter hides valid compatible donors | `seekers/views.py` |
| 2 | `O−` Universal Donor gets no special priority in emergencies | `blood_requests/views.py` |
| 3 | `AB+` Universal Recipient pool collapsed to 1 type instead of 8 | `seekers/views.py` |
| 4 | RH factor asymmetry rule (`RH−` can&#39;t receive `RH+`) not implemented | `donors/models.py` |
| 5 | No ranking — first result is no better than last | `blood_requests/views.py` |

---

### 🔬 Blood Compatibility Matrix

&gt; ✅ = Can donate · `●` = Exact match · `·` = Incompatible

| Donor ↓ \ Recipient → | O− | O+ | A− | A+ | B− | B+ | AB− | AB+ |
|---|---|---|---|---|---|---|---|---|
| **O−** | ● | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **O+** | · | ● | · | ✅ | · | ✅ | · | ✅ |
| **A−** | · | · | ● | ✅ | · | · | ✅ | ✅ |
| **A+** | · | · | · | ● | · | · | · | ✅ |
| **B−** | · | · | · | · | ● | ✅ | ✅ | ✅ |
| **B+** | · | · | · | · | · | ● | · | ✅ |
| **AB−** | · | · | · | · | · | · | ● | ✅ |
| **AB+** | · | · | · | · | · | · | · | ● |

**Key rules to encode:**
- `RH−` patients → can **only** receive `RH−` blood (irreversible sensitization risk)
- `RH+` patients → can safely receive both `RH−` and `RH+`
- `O−` → universal donor, highest emergency priority
- `AB+` → universal recipient, most restricted as a donor

---

### ✅ Proposed Solution

#### 1. New Utility — `utils/blood_compatibility.py`

```python
class BloodCompatibility:
    UNIVERSAL_DONOR = (&#39;O&#39;, &#39;-&#39;)       # Donates to all
    UNIVERSAL_RECIPIENT = (&#39;AB&#39;, &#39;+&#39;)  # Receives from all

    DONATION_COMPATIBILITY = {
        (&#39;O&#39;, &#39;-&#39;): [all 8 types],
        (&#39;O&#39;, &#39;+&#39;): [(&#39;O&#39;,&#39;+&#39;), (&#39;A&#39;,&#39;+&#39;), (&#39;B&#39;,&#39;+&#39;), (&#39;AB&#39;,&#39;+&#39;)],
        # ... full matrix
    }

    @classmethod
    def get_compatible_donors(cls, blood_group, rh_factor): ...

    @classmethod
    def get_donation_priority(cls, donor_blood, patient_blood) -&gt; int: ...

    @classmethod
    def rank_donors(cls, donors, patient_blood_group, patient_rh) -&gt; list: ...
```

#### 2. Priority Scoring — 100-point scale

| Scenario | Score |
|---|---|
| Exact same blood type | **100** |
| Universal Donor (`O−`) to non-`O−` patient | **90** |
| Same RH, different ABO group | **80** |
| Compatible but cross-RH or non-ideal | **60** |
| Incompatible | **0** (excluded) |

#### 3. Final Ranking Formula

```
final_score = (compatibility_score × 0.70) + (proximity_score × 0.30)
```

- `proximity_score = max(0, 100 - (distance_km / max_radius × 100))`
- Default radius: **50 km** (configurable)

#### 4. New Methods on `BloodRequest` model

- `get_compatible_donors()` → replaces exact-match, queries all biologically compatible types
- `get_ranked_donors(distance_km=50)` → returns donors sorted by `final_score` descending

#### 5. UI Changes — `templates/requests/detail.html`

Each donor card must show:
- Donor name + blood type badge
- Compatibility % (color-coded: 🟢 100 · 🟡 80+ · ⚪ 60)
- Distance in km
- Final score / 100
- Empty state when no donors found in range

---

### 📁 Files to Create / Modify

**New**

| File | Purpose |
|---|---|
| `utils/__init__.py` | Make `utils/` a Python package |
| `utils/blood_compatibility.py` | Core matrix + scoring + ranking engine |
| `blood_requests/tests/test_compatibility.py` | Full test suite — 64 combinations |

**Modified**

| File | Change |
|---|---|
| `blood_requests/models.py` | Add `get_compatible_donors()` + `get_ranked_donors()` to `BloodRequest` |
| `blood_requests/views.py` | Pass ranked donors to `request_detail` context |
| `templates/requests/detail.html` | Ranked donor card section with scoring UI |
| `seekers/views.py` | Replace exact-match in `donor_search()` with compatibility filter |
| `seekers/forms.py` | Optionally add distance radius field to `DonorSearchForm` |
| `donors/models.py` | Confirm `can_donate()` 90-day cooldown respected in new queries |

---

### 🧪 Acceptance Criteria

**Core Logic**
- [ ] `BloodCompatibility` class with full 8-type matrix
- [ ] `get_compatible_donors()` returns correct types for all 8 patient blood types
- [ ] `get_donation_priority()` returns accurate scores per table above
- [ ] `get_ranked_donors()` sorts by `final_score` descending

**Integration**
- [ ] `BloodRequest.get_compatible_donors()` replaces existing exact-match filter
- [ ] `can_donate()` 90-day cooldown respected in all new queries
- [ ] `donor_search()` in `seekers/views.py` uses compatibility logic

**UI**
- [ ] Donor cards show name, type, score, distance, compatibility %
- [ ] Empty state handled when no donors found within radius

**Tests — `test_compatibility.py`**
- [ ] All 64 combinations tested (8 donors × 8 recipients)
- [ ] `O−` confirmed compatible with all 8 recipient types
- [ ] `AB+` confirmed compatible only with `AB+` as donor
- [ ] `RH−` patient correctly rejects all `RH+` donors
- [ ] `rank_donors()` returns list sorted highest score first
- [ ] Distance cutoff correctly excludes out-of-range donors
- [ ] No regression on existing donor/seeker dashboard views

---

### 🔗 Reference Files

&gt; Review these before starting to understand the current codebase:

- `blood_requests/models.py` — `BloodRequest` + `DonorResponse` models
- `donors/models.py` — `DonorProfile` with `can_donate()` 90-day check
- `seekers/views.py` — current naive `donor_search()` implementation
- `users/models.py` — `CustomUser` with `latitude` / `longitude` for distance calc

---

### 💬 Notes for Contributors

&gt; ⚠️ Check if `utils/geolocation.py` with `haversine_distance()` already exists before creating it.

&gt; ⚠️ Do **not** break `request_list` view or `requests_json` API endpoint while modifying `blood_requests/views.py`.

&gt; 💡 Build the compatibility engine first. UI changes are secondary.

---
## 🎓 Program Contribution

This issue is part of:

**NSoC (Nexus Spring of Code) 🧑‍💻**

Focus areas:

* Architecture
* Deployment correctness
* Real-world scalability

---

## 🏷️ Labels

`bug` `architecture` `deployment` `high-priority` `NSoC26` `nsoc` `nsoc26`


Submitted as part of Open Source Contribution — `NSoC (Nexus Spring of Code)`

---

---

Let me first read all the reference files to understand the current codebase.

---

Good. Now I have a full picture of the codebase. Let me check if a `utils/` directory or `geolocation.py` already exists.

---

No existing `utils/` package. Here's the full plan before I write anything:

---

**Plan**

1. Create `utils/__init__.py` + `utils/blood_compatibility.py` — core matrix, scoring, haversine distance, ranking
2. Modify `blood_requests/models.py` — add `get_compatible_donors()` and `get_ranked_donors()` to `BloodRequest`
3. Modify `blood_requests/views.py` — pass ranked donors to `request_detail` context
4. Modify `seekers/views.py` — replace exact-match with compatibility filter in `donor_search()`
5. Modify `seekers/forms.py` — add `radius_km` field to `DonorSearchForm`
6. Modify `templates/requests/detail.html` — ranked donor cards with score/distance/compatibility UI
7. Create `blood_requests/tests/test_compatibility.py` — 64-combination test suite

---

---

Create utils package init file

---

Create utils directory and blood_requests/tests directory

---

shell

---

Create utils package init file

---

Create blood_requests/tests init file

---

Create the core blood compatibility engine with matrix, scoring, haversine distance, and ranking

---

Now add `get_compatible_donors()` and `get_ranked_donors()` to `BloodRequest`:

---

Add get_compatible_donors and get_ranked_donors methods to BloodRequest model

---

Now update `blood_requests/views.py` — pass ranked donors to `request_detail`:

---

Pass ranked donors to request_detail template context

---

Now update `seekers/views.py` — replace exact-match with compatibility filter:

---

Replace exact-match donor search with compatibility-aware ranked search

---

Now add `radius_km` to `DonorSearchForm`:

---

Add radius_km field to DonorSearchForm

---

Now update `templates/requests/detail.html` to show ranked donor cards:

---

Add ranked donor cards section with compatibility score, distance, and final score to detail template

---

Now create the test suite:

---

Create full 64-combination test suite for blood compatibility engine

---


All files are written. Here's a summary of every change made:

---

**New files**

- `utils/__init__.py` — makes `utils/` a Python package
- `utils/blood_compatibility.py` — the core engine:
  - `DONATION_MATRIX` — full 8×8 compatibility table
  - `get_compatible_donor_types(patient_bg, patient_rh)` — returns all donor types that can safely donate to a patient
  - `get_donation_priority(donor, patient)` → 100 / 90 / 80 / 60 / 0 scoring
  - `haversine_distance()` — great-circle distance in km
  - `rank_donors()` — filters by radius, scores by `(compat×0.70) + (proximity×0.30)`, returns sorted list of dicts
- `blood_requests/tests/__init__.py`
- `blood_requests/tests/test_compatibility.py` — 64-combination test suite covering all matrix cells, O- universal donor, AB+ universal recipient, RH- rejection, scoring tiers, distance cutoff, and ranking order

**Modified files**

- `blood_requests/models.py` — added `get_compatible_donors()` (compatibility-filtered queryset) and `get_ranked_donors(radius_km=50)` (calls `rank_donors`, respects `can_donate()` 90-day cooldown) to `BloodRequest`
- `blood_requests/views.py` — `request_detail` now passes `ranked_donors` to the template; `request_list` and `requests_json` are untouched
- `seekers/views.py` — `donor_search()` now uses `get_compatible_donor_types()` + `rank_donors()` when both blood group and RH factor are provided; falls back gracefully when only partial filters are given
- `seekers/forms.py` — added `radius_km` field (default 50, max 500) to `DonorSearchForm`
- `templates/requests/detail.html` — new ranked donor cards section showing blood type badge, name, distance, colour-coded compatibility % (🟢100 / 🟡80+ / ⚪60), and final score; empty state when no donors found